import { View, Button } from '../Custom';

function AdopButton() {
  return (
    <View className="mb-4">
      <Button title="Adopt me" onPress={() => {}} />
    </View>
  );
}

export default AdopButton;
